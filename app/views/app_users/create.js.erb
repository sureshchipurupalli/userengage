<% if @errors.present? && @errors.size > 0  %>
$("#create_app_user").html("<%= j render 'shared/errors', errors: @errors %>");
<% else %>
window.location = "<%= app_users_path + '?app_id=' + @app_id.to_s%>";

<% end %>
