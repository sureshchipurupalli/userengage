-if errors.present? && errors.size > 0
  .alert.alert-danger{"role"=>"alert","id"=>"alerts","style"=>"text-align:center"}
    - errors.each do |error|
      %li
        = error.kind_of?(Array) ? error.to_sentence : error