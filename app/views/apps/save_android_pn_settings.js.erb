<% if @errors.present? && @errors.size > 0  %>
  $("#android_rowalerts").html("<%= j render 'shared/errors', errors: @errors %>");
<% else %>
  $("#android_rowalerts").html("<%= j render 'shared/success', display_text: 'Android push notification settings updated successfully.' %>");
<% end %>
